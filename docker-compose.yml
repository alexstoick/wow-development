version: '2'
services:
  datafetch:
    restart: always
    build: ./datafetch/
    depends_on:
      - db
  web:
    build: ./web/
    ports:
      - "3000:3000"
    depends_on:
      - db
  db:
    image: postgres
    environment:
      - POSTGRES_DB=wow_development
    volumes:
      - data:/var/lib/postgresql
volumes:
  data: {}
